// Pro features are enabled locally — these components are no-ops.

export function ProBanner() {
  return null;
}

export function SetupDyadProButton({
  className: _className,
}: {
  className?: string;
}) {
  return null;
}

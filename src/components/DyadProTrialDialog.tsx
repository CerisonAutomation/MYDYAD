// Dyad Pro trial dialog — disabled in Agent2 mode (no subscription needed).
export function DyadProTrialDialog({
  isOpen: _isOpen,
  onClose: _onClose,
}: {
  isOpen: boolean;
  onClose: () => void;
  utmCampaign?: string;
}) {
  return null;
}

/**
 * DyadProTrialDialog removed — no subscription trials in this build.
 */
export function DyadProTrialDialog(_props: {
  isOpen: boolean;
  onClose: () => void;
  utmCampaign?: string;
}) {
  return null;
}
